/** Shell switches between mobile app chrome and desktop dashboard at this width. */
export const DESKTOP_SHELL_MIN_WIDTH = 1024;

export const desktopShellMediaQuery = `(min-width: ${DESKTOP_SHELL_MIN_WIDTH}px)`;
